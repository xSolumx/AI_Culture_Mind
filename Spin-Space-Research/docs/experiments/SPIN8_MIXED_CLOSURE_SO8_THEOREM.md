# Mixed Monomial--Golden Closure Is Dense in SO(8)

**Updated:** 2026-08-21T14:29:36+02:00
**Status:** exact topological-closure theorem for one fixed eleven-generator system in the maintained eight-dimensional carrier.

## Theorem

Let \(H\) be generated by the seven associative octonion left-multiplication matrices, two signed Fano-basis automorphism generators, and the golden vector `a,b` pair used by the maintained `2.A5` embedding. Then

\[
\overline H = SO(8).
\]

Equivalently, this explicitly specified mixed subgroup is dense in \(SO(8)\). Adding the positive- and negative-half-spin golden generators cannot make the all-three-view mixed system smaller, so its closure is also \(SO(8)\).

## Exact proof certificate

All acceptance checks are finite exact arithmetic over \(\mathbb Q(\sqrt5)\), or integer arithmetic for the monomial subgroup.

1. The word `golden_vector_b*(fano_automorphism_b*(left_operator_e1))` has a non-cyclotomic quartic characteristic factor. It has infinite order, so the compact closure has nonzero Lie algebra.
2. The signed-monomial normalizer has order 21,504. Its conjugation action on \(\mathfrak{so}(8)\) splits as \(V_7\oplus V_{21}\), represented exactly by the seven Clifford grade-one matrices \(L_{e_i}\) and their 21 grade-two products \(L_{e_i}L_{e_j}\).
3. Exact character inner products are \(\langle\chi_7,\chi_7\rangle=1\), \(\langle\chi_{21},\chi_{21}\rangle=1\), and \(\langle\chi_7,\chi_{21}\rangle=0\). A normalizer-invariant Lie algebra can only be \(0,V_7,V_{21}\), or all of \(\mathfrak{so}(8)\).
4. \([L_{e_1},L_{e_2}]=2L_{e_1}L_{e_2}\), so \(V_7\) is not Lie closed.
5. Conjugating \(L_{e_1}L_{e_4}\) by the infinite word gives grade-one Frobenius coefficient \(-1/4\) on \(L_{e_1}\). Hence that word does not normalize \(V_{21}\), ruling it out as the identity-component Lie algebra.

Only the full \(\mathfrak{so}(8)\) remains. A closed subgroup of connected \(SO(8)\) with Lie algebra \(\mathfrak{so}(8)\) is \(SO(8)\) itself.

## Replay

```powershell
Set-Location C:\Users\HaydenLocal\Programming\AI_Culture_Mind\Spin-Space-Research
$env:PYTHONPATH = "src"
python src\spin8_mixed_closure_so8_theorem.py `
  --output artifacts\spin8_mixed_closure_so8_theorem_20260821.json
python -m pytest -q tests\test_spin8_mixed_closure_so8_theorem.py
```

## Boundary

This proves density for this fixed generator system. It does not claim a new abstract group, an arbitrary learned Spin(8) parameterization, a finite quotient classification, or an ML/SSM performance advantage. The earlier infinitude-only report is retained as the discovery-stage certificate and is superseded on its closure-status boundary by this theorem.
