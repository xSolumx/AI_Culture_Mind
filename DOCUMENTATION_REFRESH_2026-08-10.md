# Repository-wide documentation refresh, 2026-08-10

> **Topology update, 2026-08-11:** this report records the pre-flattening
> parent/linked-repository layout. `Spin-Space-Research/` is now an ordinary
> root-owned directory; current navigation is defined by
> [`REPOSITORY_MAP.md`](REPOSITORY_MAP.md).

## Scope

This pass audited every Markdown file visible from the parent workspace and the
linked `Spin-Space-Research` repository. The scan covered 368 Markdown
files. Dependency lists and corpus `.txt` files were classified as inputs, not
documentation. The active worktree contains 60 changed or new Markdown files
across the research campaign and this consistency pass.

The goal was scientific consistency, not retroactive rewriting. Live
documentation was updated directly. Dated preregistrations, result reports,
and extracted archives retain their original observations and receive a
current-status pointer when a later result changes their roadmap or
interpretation.

The later [research-programme refactor](research-programs/README.md) further
separates scan/compiler algebra, equivariant identification, structured memory,
exceptional representation dynamics, information geometry, rotor SSMs, and the
imported Collatz proposal. Historical programme labels are retained in dated
reports as provenance; they no longer control cross-program claim transfer.

## Canonical 2026-08-10 memory boundary

- Direct and triality-coded slots have the same ordinary overwrite capacity
  when they receive the same router and value transport.
- Spin(8) triality remains useful as a shared cross-view action and routing
  prior; no matched-state storage advantage is claimed.
- A prospective paired-action Task B replication on untouched seeds `20`--`29`
  closes the last direct/delta action row `10/10`. Shared actions remain exact;
  the parameter-richer independent family averages `0.542924` at length 2,048;
  routing-matched controls leave the gap unchanged. The earlier strict replay
  remains a failed provenance result because its source artifact did not retain
  learned parameters.
- NSA contributes a coarse-to-fine routing pattern, not Schur or triality
  algebra. Hierarchical block routing improves learned direct and delta
  retrieval in the frozen synthetic campaign.
- A later frozen campaign extends this to 64 overlapping keys: shared routing
  and hierarchy pass `10/10`, actual eager gathering preserves the recurrence
  but loses to dense through launch overhead, and a separately preregistered
  fused gather beats both eager controls on the named GPU.
- A final protocol audit caught a fixed-per-cell timing order where the frozen
  eager protocol required blockwise cycling. The scheduler and aggregate
  validators were corrected, all six timing processes were rerun, and only the
  corrected artifacts and numbers are documented.
- General invertible value transport admits a stable co-moving reduction to a
  standard DeltaRule recurrence. Official FLA chunk/recurrent measurements
  include prefix, solve, and read-frame costs.
- The recorded length-4096 RTX 2070 SUPER comparison is about `5.20x` faster
  forward and `5.30x` faster forward plus backward than the direct transported
  reference, with 128 rather than 64 logical recurrent-state scalars.
- Spin(9) supplies exact 9-to-16 Clifford bind/unbind and a candidate Hopf
  coarse index. Width expansion prevents a same-state capacity claim.
- The unrestricted Spin(8) Dirac--Gram theorem is independent and is not a
  prerequisite for these memory and systems results.

The earlier Spin(8)/Spin(9) and transported-FLA report is
[`SPIN8_SPIN9_HIERARCHICAL_MEMORY_RESULTS.md`](Spin-Space-Research/docs/experiments/SPIN8_SPIN9_HIERARCHICAL_MEMORY_RESULTS.md).
The current large-slot and physical-gather report is
[`LARGE_SLOT_SEMANTIC_HIERARCHY_RESULTS.md`](Spin-Space-Research/docs/experiments/LARGE_SLOT_SEMANTIC_HIERARCHY_RESULTS.md).
The protocol-separated visual synthesis and current FLA layer boundary are in
[`MEMORY_BENCHMARK_ATLAS.md`](Spin-Space-Research/docs/experiments/MEMORY_BENCHMARK_ATLAS.md).
The selected-block mechanism remains a standalone inference kernel rather than
an FLA/Hugging Face mixer with cache and backward contracts.
The Task B evidence correction and prospective close are in
[`TASK_B_DELTA_ACTION_REPLAY_RESULTS.md`](Spin-Space-Research/docs/experiments/TASK_B_DELTA_ACTION_REPLAY_RESULTS.md)
and
[`TASK_B_PAIRED_ACTION_REPLICATION_RESULTS.md`](Spin-Space-Research/docs/experiments/TASK_B_PAIRED_ACTION_REPLICATION_RESULTS.md).

## Documentation surfaces synchronized

- parent and theorem-repository front doors;
- research-program indexes and the Spin(8), Spin(9), memory, and benchmark
  ledgers;
- mathematical foundations and plain-language explanation;
- experiment index, source index, and reproducibility commands;
- publication and non-transfer policies;
- SSM/Spinor model lineage and the isolated benchmark boundary;
- literature, manuscript, foundational, gate, and documentation audits;
- historical SchurScan, coded-memory, learned-address, and continuous-alias
  entry points.

## Validation

- 232 theorem-repository Markdown files passed the maintained mechanical and
  local-link audit;
- zero missing local Markdown links, unbalanced fenced-code delimiters, or
  unbalanced `\\[` / `\\]` display-math delimiters in that audit;
- `git diff --check` passed in both the parent and nested repositories.
- focused memory/compiler replay: 41 tests and 22 subtests passed;
- all 23 new large-slot/gather artifacts parse and match their SHA-256 entries.

The repository-wide artifact completeness audit separately reports 20
unmanifested Dirac-octet runtime artifacts from another active workstream. They
were not added to this memory campaign's manifest scope.

This validation checks documentation integrity. It does not independently
re-run every experiment or upgrade any empirical result to a theorem.
