# Repository-wide documentation refresh, 2026-08-10

> **Historical status.** This refresh remains the 2026-08-10 record. Its
> maintained-model and benchmark successor is
> [`DOCUMENTATION_REFRESH_2026-08-16.md`](DOCUMENTATION_REFRESH_2026-08-16.md),
> which preserves this document's 2026-08-10 observations while recording later
> Pure Rotor scan and benchmark provenance.

## Scope

This pass audited every Markdown file visible from the parent workspace and the
linked `Spin8-Triality-Research` repository. The scan covered 368 Markdown
files. Dependency lists and corpus `.txt` files were classified as inputs, not
documentation. The active worktree contains 60 changed or new Markdown files
across the research campaign and this consistency pass.

The goal was scientific consistency, not retroactive rewriting. Live
documentation was updated directly. Dated preregistrations, result reports,
and extracted archives retain their original observations and receive a
current-status pointer when a later result changes their roadmap or
interpretation.

## Canonical 2026-08-10 memory boundary

- Direct and triality-coded slots have the same ordinary overwrite capacity
  when they receive the same router and value transport.
- Spin(8) triality remains useful as a shared cross-view action and routing
  prior; no matched-state storage advantage is claimed.
- NSA contributes a coarse-to-fine routing pattern, not Schur or triality
  algebra. Hierarchical block routing improves learned direct and delta
  retrieval in the frozen synthetic campaign.
- General invertible value transport admits a stable co-moving reduction to a
  standard DeltaRule recurrence. Official FLA chunk/recurrent measurements
  include prefix, solve, and read-frame costs.
- The recorded length-4096 RTX 2070 SUPER comparison is about `5.20x` faster
  forward and `5.30x` faster forward plus backward than the direct transported
  reference, with 128 rather than 64 logical recurrent-state scalars.
- Spin(9) supplies exact 9-to-16 Clifford bind/unbind and a candidate Hopf
  coarse index. Width expansion prevents a same-state capacity claim.
- The unrestricted Spin(8) Dirac--Gram theorem is independent and is not a
  prerequisite for these memory and systems results.

The authoritative technical report is
[`SPIN8_SPIN9_HIERARCHICAL_MEMORY_RESULTS.md`](Spin8-Triality-Research/docs/experiments/SPIN8_SPIN9_HIERARCHICAL_MEMORY_RESULTS.md).

## Documentation surfaces synchronized

- parent and theorem-repository front doors;
- research-program indexes and the Spin(8), Spin(9), memory, and benchmark
  ledgers;
- mathematical foundations and plain-language explanation;
- experiment index, source index, and reproducibility commands;
- publication and non-transfer policies;
- SSM/Spinor model lineage and the isolated benchmark boundary;
- literature, manuscript, foundational, gate, and documentation audits;
- historical SchurScan, coded-memory, learned-address, and continuous-alias
  entry points.

## Validation

- 368 Markdown files scanned;
- zero missing local Markdown links;
- zero unbalanced fenced-code delimiters;
- zero unbalanced `\\[` / `\\]` display-math delimiters;
- `git diff --check` passed in both the parent and nested repositories.
- focused memory/compiler replay: 40 tests and 83 subtests passed.

This validation checks documentation integrity. It does not independently
re-run every experiment or upgrade any empirical result to a theorem.
